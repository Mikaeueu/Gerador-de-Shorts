# ============================================================
# Gerador de Shorts — Dependências
# ============================================================
# As dependências são adicionadas conforme cada etapa é implementada.
# Comente/descomente conforme avança no roadmap.

# ---- Etapa 1: Downloader ----
yt-dlp>=2024.10.0

# ---- Etapa 2: Transcrição ----
faster-whisper>=1.0.3

# ---- Etapa 3: Análise viral via LLM ----
google-genai>=0.7.0      # SDK novo unificado do Google (substitui google-generativeai)
groq>=0.11.0             # Llama 3.3 70B — usado pelo refiner (Etapa 2.5) + fallback chain
openai>=1.50.0           # GPT-4o-mini (fallback opcional — só usado se Groq+Gemini falharem)
python-dotenv>=1.0.0     # carrega .env
pydantic>=2.9.0          # schema das respostas estruturadas

# ---- Etapa 4: Reenquadramento ----
opencv-python>=4.5.4     # detecção de rosto via YuNet (cv2.FaceDetectorYN)
                         # + Haar Cascade fallback + leitura de frames
numpy>=1.26.0            # arrays pra suavização da trajetória
# Modelo YuNet (~340KB) é baixado automaticamente em data/models/
# na primeira execução. Ver src/cropper/face_tracker.py.
# MediaPipe foi removido — YuNet substitui com vantagens (mais preciso,
# sem inferno de versões).

# ---- Etapa 5/6: Legendas e exportação ----
# (FFmpeg é binário do sistema, não dependência Python)

# ---- Etapa 7: API ----
fastapi>=0.115.0
uvicorn[standard]>=0.32.0     # standard inclui websockets, watchgod, etc
python-multipart>=0.0.12      # parsing de uploads multipart/form-data
# pydantic ja vem instalado pela Etapa 3
